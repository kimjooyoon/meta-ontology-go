# Language package runtime corpus

This versioned corpus fixes the denominator at 18 cases: 10 deterministic
runtime-image paths and 8 fail-closed package boundaries. The witness owns
the canonical four-package, five-source fixture; this file owns which
observations count and the exact expected rejection codes.
