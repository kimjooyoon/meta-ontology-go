# Conformance fixture

`main.gooo` is a minimal independent fixture for the parser, semantic lowerer,
and generator. Run it using the commands in [docs/conformance.md](../../docs/conformance.md).

Its stable IDs are deliberately different from the billing example so tests can
detect accidental name- or namespace-based coupling.
