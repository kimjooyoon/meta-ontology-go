# Language Go Interoperation Example

This example names the fixed use cases consumed by the Go interoperation metaprogram. CI, not this directory, is the conformance authority.

The metaprogram executes `8` real generator projections, `8` Go 1.27 API fixtures, and `8` expected rejections. It never reads these example files as hidden runtime input.

See `plan.json` for the versioned denominator and the exact successful values.
