# Toolchain LSP corpus

This versioned corpus has exactly 22 cases: 16 protocol cases and 6 semantic-coupling cases.

The witness executes the existing dependency-free Gooo LSP server through Content-Length-framed JSON-RPC. It also executes the immutable coupling adapter. UNKNOWN, FAIL_CLOSED, stale, and cancelled coupling states must return zero navigation links.

The denominator changes only with a new corpus schema. The witness reads source and evidence, writes only its requested external artifact, and has no repository mutation authority.
