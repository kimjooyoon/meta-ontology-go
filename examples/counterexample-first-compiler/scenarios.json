{
  "schema": "gooo/counterexample-first-scenarios/v3",
  "version": 1,
  "scenarios": [
    {
      "id": "resolved-minimal-counterexample",
      "candidate": {
        "id": "candidate-identity-drift-long",
        "claim_id": "claim-resolved-repair",
        "predicate_id": "identity-drift-detected",
        "claim": "the candidate identity drift can be repaired by canonicalizing the same minimal source",
        "source": "package counterexamplefirst\nnamespace counterexamplefirst\n\nentity CompilationClaim id \"gooo://counterexample-first/entity/compilation-claim?noise=1&drift=1\"\nentity MinimalCounterexample id \"gooo://counterexample-first/entity/minimal-counterexample\"\nentity ResolutionEvidence id \"gooo://counterexample-first/entity/resolution-evidence\"\nentity CompilationDecision id \"gooo://counterexample-first/entity/compilation-decision\"\n\nactivity CanonicalEntityID(CompilationClaim) -> CompilationClaim computes \"identity:v1\"\nactivity DiscoverMinimalCounterexample(CompilationClaim) -> MinimalCounterexample\nactivity BindResolutionEvidence(MinimalCounterexample) -> ResolutionEvidence\nactivity PromoteOnlyAfterResolution(ResolutionEvidence) -> CompilationDecision\n"
      },
      "resolution_input": {"id":"resolution-replay-canonical","operation":"canonicalize-entity-id"}
    },
    {
      "id": "canonical-control",
      "candidate": {
        "id": "candidate-canonical-source",
        "claim_id": "claim-canonical-control",
        "predicate_id": "canonical-source-admissible",
        "claim": "a canonical candidate has no observed identity violation but is insufficient evidence for promotion",
        "source": "package counterexamplefirst\nnamespace counterexamplefirst\n\nentity CompilationClaim id \"gooo://counterexample-first/entity/compilation-claim\"\nentity MinimalCounterexample id \"gooo://counterexample-first/entity/minimal-counterexample\"\nentity ResolutionEvidence id \"gooo://counterexample-first/entity/resolution-evidence\"\nentity CompilationDecision id \"gooo://counterexample-first/entity/compilation-decision\"\n\nactivity CanonicalEntityID(CompilationClaim) -> CompilationClaim computes \"identity:v1\"\nactivity DiscoverMinimalCounterexample(CompilationClaim) -> MinimalCounterexample\nactivity BindResolutionEvidence(MinimalCounterexample) -> ResolutionEvidence\nactivity PromoteOnlyAfterResolution(ResolutionEvidence) -> CompilationDecision\n"
      }
    },
    {
      "id": "unresolved-counterexample",
      "candidate": {
        "id": "candidate-identity-drift",
        "claim_id": "claim-unresolved-boundary",
        "predicate_id": "resolution-required",
        "claim": "an identity violation without repair evidence remains refuted",
        "source": "package counterexamplefirst\nnamespace counterexamplefirst\n\nentity CompilationClaim id \"gooo://counterexample-first/entity/compilation-claim?drift=1\"\nentity MinimalCounterexample id \"gooo://counterexample-first/entity/minimal-counterexample\"\nentity ResolutionEvidence id \"gooo://counterexample-first/entity/resolution-evidence\"\nentity CompilationDecision id \"gooo://counterexample-first/entity/compilation-decision\"\n\nactivity CanonicalEntityID(CompilationClaim) -> CompilationClaim computes \"identity:v1\"\nactivity DiscoverMinimalCounterexample(CompilationClaim) -> MinimalCounterexample\nactivity BindResolutionEvidence(MinimalCounterexample) -> ResolutionEvidence\nactivity PromoteOnlyAfterResolution(ResolutionEvidence) -> CompilationDecision\n"
      }
    },
    {
      "id": "comment-only-control",
      "candidate": {
        "id": "candidate-comment-only",
        "claim_id": "claim-comment-invariance",
        "predicate_id": "semantic-digest-invariant",
        "claim": "comment-only source changes preserve semantic lowering and predicate evidence",
        "source": "package counterexamplefirst\nnamespace counterexamplefirst\n\n// comment-only intervention: no semantic token changes\nentity CompilationClaim id \"gooo://counterexample-first/entity/compilation-claim\"\nentity MinimalCounterexample id \"gooo://counterexample-first/entity/minimal-counterexample\"\nentity ResolutionEvidence id \"gooo://counterexample-first/entity/resolution-evidence\"\nentity CompilationDecision id \"gooo://counterexample-first/entity/compilation-decision\"\n\nactivity CanonicalEntityID(CompilationClaim) -> CompilationClaim computes \"identity:v1\"\nactivity DiscoverMinimalCounterexample(CompilationClaim) -> MinimalCounterexample\nactivity BindResolutionEvidence(MinimalCounterexample) -> ResolutionEvidence\nactivity PromoteOnlyAfterResolution(ResolutionEvidence) -> CompilationDecision\n"
      }
    },
    {
      "id": "unobserved-input",
      "candidate": {
        "id": "candidate-source-not-provided",
        "claim_id": "claim-source-acquisition",
        "predicate_id": "source-acquisition-present",
        "claim": "absent source acquisition retains UNKNOWN rather than inferring a decision",
        "source": null
      }
    }
  ]
}
