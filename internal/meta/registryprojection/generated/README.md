# Manual-source-registration-edit-free registry projection

This bounded projection is generated from local `concept.manifest.json` inputs. Existing concepts own their manifests; catalog, corpus, registry, fixed denominator, and this document are projections.

The root topology and root README remain outside this slice. The legacy global catalog remains readable while migration is intentionally bounded to these three concepts.

| Stable ID | Stage | Meta operation | Denominator entries | Verification strategies |
| --- | --- | --- | ---: | --- |
| `language-semantic-model` | `OPERATING` | `prove-staged-semantic-model` | 2 | `COHERENCE, FOUNDATION, REGRESSION` |
| `language-syntax-roundtrip` | `OPERATING` | `prove-language-syntax-roundtrip` | 1 | `COHERENCE, FOUNDATION, REGRESSION` |
| `toolchain-conformance` | `OPERATING` | `close-toolchain-conformance-ledger` | 1 | `COHERENCE, FOUNDATION, REGRESSION` |

`language-semantic-model` — syntax expansion can blur semantic authority and ambient effects → normalized IR replay separates meaning, provenance, evidence, and sealed effects

`language-syntax-roundtrip` — syntax examples can exist without proving AST, byte, semantic, and lens preservation → a complete versioned corpus emits a deterministic external preservation receipt

`toolchain-conformance` — independent green tools can disagree on schema, commit identity, evidence, or effect authority → a versioned metaprogram closes nine exact-head tool receipts and rejects bounded drift in memory

## Local ownership

The projection currently contains 3 local manifests. Manifest order is not semantic; generation sorts by stable ID.

Generated by the manual-source-registration-edit-free projection metaprogram under Go 1.27.0.
