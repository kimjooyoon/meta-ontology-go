name: Language value witness

on:
  pull_request:
    branches: [dev]
    paths:
      - .github/workflows/language-value-witness.yml
      - examples/language-value-witness/**
      - examples/language-runtime-binding/**
      - examples/language-syntax-roundtrip/corpus.json
      - internal/bidir/**
      - internal/semantic/**
      - internal/meta/languagereadiness/languagesyntax/**
      - internal/syntax/**
      - internal/valueexecution/**
      - scripts/language-value-witness/**
  push:
    branches: [dev]
    paths:
      - .github/workflows/language-value-witness.yml
      - examples/language-value-witness/**
      - examples/language-runtime-binding/**
      - examples/language-syntax-roundtrip/corpus.json
      - internal/bidir/**
      - internal/semantic/**
      - internal/meta/languagereadiness/languagesyntax/**
      - internal/syntax/**
      - internal/valueexecution/**
      - scripts/language-value-witness/**
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prove-value-level-computation:
    runs-on: ubuntu-latest
    env:
      HEAD_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.HEAD_SHA }}
      - uses: actions/setup-go@v7
        with:
          go-version: 1.27.x
          check-latest: true
      - name: Go 1.27 fix and canonical format conformance
        shell: bash
        run: |
          set -euo pipefail
          go fix ./internal/semantic ./internal/bidir ./internal/valueexecution ./scripts/language-value-witness
          gofmt -w \
            internal/syntax/ast_part03.go \
            internal/syntax/activity_parser.go \
            internal/syntax/format_part04.go \
            internal/syntax/format_value_program.go \
            internal/syntax/value_program_test.go \
            internal/bidir/lower_part05.go \
            internal/bidir/lower_document_part03.go \
            internal/bidir/lower_value_program.go \
            internal/bidir/value_program.go \
            internal/bidir/value_program_test.go \
            internal/semantic/types_part02.go \
            internal/semantic/types_part03.go \
            internal/semantic/node_value_program.go \
            internal/semantic/canonical_part01.go \
            internal/valueexecution/model.go \
            internal/valueexecution/model_evidence.go \
            internal/valueexecution/model_summary.go \
            internal/valueexecution/failure.go \
            internal/valueexecution/digest.go \
            internal/valueexecution/registry.go \
            internal/valueexecution/compile.go \
            internal/valueexecution/program.go \
            internal/valueexecution/evaluate.go \
            internal/valueexecution/evaluation.go \
            internal/valueexecution/measurement.go \
            internal/valueexecution/core_ir_measurement.go \
            internal/valueexecution/cases.go \
            internal/valueexecution/counterexamples.go \
            internal/valueexecution/counterexample_results.go \
            internal/valueexecution/helpers.go \
            internal/valueexecution/indicators.go \
            internal/valueexecution/indicator_results.go \
            internal/valueexecution/views.go \
            internal/valueexecution/proofs.go \
            internal/valueexecution/validate.go \
            internal/valueexecution/evaluate_test.go \
            scripts/language-value-witness/main.go
          git diff --exit-code -- \
            internal/syntax internal/bidir internal/semantic internal/valueexecution \
            scripts/language-value-witness
      - name: Compile parser, semantic binding, and value executor
        run: go test ./internal/syntax ./internal/bidir ./internal/semantic ./internal/valueexecution
      - name: Dogfood the int.sign Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-sign-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/sign.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/sign.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo int.sign dogfood"
            echo '- declaration: `examples/language-value-witness/sign.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.sign dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-sign-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-sign-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Dogfood the int.mod Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-mod-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/mod.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/mod.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo int.mod dogfood"
            echo '- declaration: `examples/language-value-witness/mod.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.mod dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-mod-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-mod-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Execute the int.mod Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-mod-execution"
          mkdir -p "$output"
          printf '{"value":41}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry Remainder \
              --input "$output/input.json" \
              examples/language-value-witness/mod.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "Remainder" and
            .source_digest != "" and
            .semantic_fingerprint != "" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.Remainder.value == 2 and
            .execution.results.Remainder.operation_spec_digest != "" and
            .execution.results.Remainder.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo int.mod execution"
            echo '- declaration: `examples/language-value-witness/mod.gooo`'
            echo '- input: `41`'
            echo '- observed result: `2`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.mod execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-mod-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-mod-execution
          if-no-files-found: error
          retention-days: 14
      - name: Dogfood the int.neg Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-neg-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/neg.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/neg.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo int.neg dogfood"
            echo '- declaration: `examples/language-value-witness/neg.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.neg dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-neg-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-neg-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Execute the int.neg Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-neg-execution"
          mkdir -p "$output"
          printf '{"value":5}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry Negate \
              --input "$output/input.json" \
              examples/language-value-witness/neg.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "Negate" and
            .source_digest != "" and
            .semantic_fingerprint != "" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.Negate.value == -5 and
            .execution.results.Negate.operation_spec_digest != "" and
            .execution.results.Negate.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo int.neg execution"
            echo '- declaration: `examples/language-value-witness/neg.gooo`'
            echo '- input: `5`'
            echo '- observed result: `-5`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.neg execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-neg-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-neg-execution
          if-no-files-found: error
          retention-days: 14
      - name: Dogfood the int.abs Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-abs-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/abs.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/abs.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo int.abs dogfood"
            echo '- declaration: `examples/language-value-witness/abs.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.abs dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-abs-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-abs-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Execute the int.abs Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-abs-execution"
          mkdir -p "$output"
          printf '{"value":-9}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry Absolute \
              --input "$output/input.json" \
              examples/language-value-witness/abs.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "Absolute" and
            .source_digest != "" and
            .semantic_fingerprint != "" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.Absolute.value == 9 and
            .execution.results.Absolute.operation_spec_digest != "" and
            .execution.results.Absolute.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo int.abs execution"
            echo '- declaration: `examples/language-value-witness/abs.gooo`'
            echo '- input: `-9`'
            echo '- observed result: `9`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.abs execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-abs-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-abs-execution
          if-no-files-found: error
          retention-days: 14
      - name: Dogfood the parameterized int.add:2 Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-add-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/add.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/add.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo int.add:2 dogfood"
            echo '- declaration: `examples/language-value-witness/add.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.add:2 dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-add-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-add-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Execute the parameterized int.add:2 Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-add-execution"
          mkdir -p "$output"
          printf '{"value":7}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry AddTwo \
              --input "$output/input.json" \
              examples/language-value-witness/add.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "AddTwo" and
            .source_digest != "" and
            .semantic_fingerprint != "" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.AddTwo.value == 9 and
            .execution.results.AddTwo.operation_spec_digest != "" and
            .execution.results.AddTwo.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo int.add:2 execution"
            echo '- declaration: `examples/language-value-witness/add.gooo`'
            echo '- input: `7`'
            echo '- observed result: `9`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.add:2 execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-add-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-add-execution
          if-no-files-found: error
          retention-days: 14
      - name: Dogfood the registered int.mul:3 Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-mul-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/mul.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/mul.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo int.mul:3 dogfood"
            echo '- declaration: `examples/language-value-witness/mul.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.mul:3 dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-mul-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-mul-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Execute the int.mul:3 Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-mul-execution"
          mkdir -p "$output"
          printf '{"value":4}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry Multiply \
              --input "$output/input.json" \
              examples/language-value-witness/mul.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "Multiply" and
            .source_digest != "" and
            .semantic_fingerprint != "" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.Multiply.value == 12 and
            .execution.results.Multiply.operation_spec_digest != "" and
            .execution.results.Multiply.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo int.mul:3 execution"
            echo '- declaration: `examples/language-value-witness/mul.gooo`'
            echo '- input: `4`'
            echo '- observed result: `12`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.mul:3 execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-mul-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-mul-execution
          if-no-files-found: error
          retention-days: 14
      - name: Dogfood the registered int.iszero:0 Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-iszero-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/iszero.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/iszero.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo int.iszero:0 dogfood"
            echo '- declaration: `examples/language-value-witness/iszero.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- zero-iszeroisor policy: `FAIL_CLOSED`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.iszero:0 dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-iszero-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-iszero-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Execute the int.iszero:0 Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-iszero-execution"
          mkdir -p "$output"
          printf '{"value":0}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry IsZero \
              --input "$output/input.json" \
              examples/language-value-witness/iszero.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "IsZero" and
            .source_digest != "" and
            .semantic_fingerprint != "" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.results.IsZero.output_entity == "Boolean" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.IsZero.value == 1 and
            .execution.results.IsZero.operation_spec_digest != "" and
            .execution.results.IsZero.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo int.iszero:0 execution"
            echo '- declaration: `examples/language-value-witness/iszero.gooo`'
            echo '- input: `-7`'
            echo '- observed result: `1`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- zero-iszeroisor policy: `FAIL_CLOSED`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload int.iszero:0 execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-iszero-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-iszero-execution
          if-no-files-found: error
          retention-days: 14
      - name: Execute the positive int.iszero:0 Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-iszero-positive-execution"
          mkdir -p "$output"
          printf '{"value":7}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry IsZero \
              --input "$output/input.json" \
              examples/language-value-witness/iszero.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "IsZero" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.results.IsZero.output_entity == "Boolean" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.IsZero.value == 0 and
            .execution.results.IsZero.operation_spec_digest != "" and
            .execution.results.IsZero.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo int.iszero:0 positive execution"
            echo '- input: `7`'
            echo '- observed result: `0`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload positive int.iszero:0 execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-iszero-positive-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-iszero-positive-execution
          if-no-files-found: error
          retention-days: 14
      - name: Dogfood the registered bool.not:0 Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-bool-not-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/not.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/not.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo bool.not:0 dogfood"
            echo '- declaration: `examples/language-value-witness/not.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- canonical Boolean input policy: `FAIL_CLOSED`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload bool.not:0 dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-bool-not-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-bool-not-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Execute the bool.not:0 Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-bool-not-execution"
          mkdir -p "$output"
          printf '{"value":0}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry Not \
              --input "$output/input.json" \
              examples/language-value-witness/not.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "Not" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.results.Not.output_entity == "Boolean" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.Not.value == 1 and
            .execution.results.Not.operation_spec_digest != "" and
            .execution.results.Not.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo bool.not:0 execution"
            echo '- declaration: `examples/language-value-witness/not.gooo`'
            echo '- input: `false (0)`'
            echo '- observed result: `true (1)`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- canonical Boolean input policy: `FAIL_CLOSED`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload bool.not:0 execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-bool-not-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-bool-not-execution
          if-no-files-found: error
          retention-days: 14
      - name: Execute the true bool.not:0 Gooo plan and replay its receipt
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-bool-not-true-execution"
          mkdir -p "$output"
          printf '{"value":1}\n' > "$output/input.json"
          for pass in first second; do
            go run ./cmd/gooo run --json --entry Not \
              --input "$output/input.json" \
              examples/language-value-witness/not.gooo > "$output/$pass.json"
          done
          cmp "$output/first.json" "$output/second.json"
          jq -e '
            .schema == "gooo/value-execution-plan/v1" and
            .decision == "PASS" and
            .entry == "Not" and
            .execution.scope == "REGISTERED_VALUE_OPERATION" and
            .execution.results.Not.output_entity == "Boolean" and
            .execution.apply_calls == 1 and
            .execution.deliveries == 0 and
            .execution.results.Not.value == 0 and
            .execution.results.Not.operation_spec_digest != "" and
            .execution.results.Not.result_digest != ""
          ' "$output/first.json" > /dev/null
          {
            echo "### Gooo bool.not:0 true execution"
            echo '- input: `true (1)`'
            echo '- observed result: `false (0)`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload true bool.not:0 execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-bool-not-true-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-bool-not-true-execution
          if-no-files-found: error
          retention-days: 14
      - name: Dogfood the bool.and:1 Gooo declaration and generated Go
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-bool-and-dogfood"
          mkdir -p "$output/first" "$output/second"
          go run ./cmd/gooo check --semantic examples/language-value-witness/and.gooo > "$output/check.txt"
          for pass in first second; do
            go run ./cmd/gooo generate examples/language-value-witness/and.gooo --out "$output/$pass"
            test -s "$output/$pass/semantic.gooo.go"
            test -s "$output/$pass/semantic.gooo.manifest.jsonl"
            test -z "$(gofmt -l "$output/$pass/semantic.gooo.go")"
          done
          cmp "$output/first/semantic.gooo.go" "$output/second/semantic.gooo.go"
          jq -S 'del(.generated_file)' "$output/first/semantic.gooo.manifest.jsonl" > "$output/first/canonical-manifest.json"
          jq -S 'del(.generated_file)' "$output/second/semantic.gooo.manifest.jsonl" > "$output/second/canonical-manifest.json"
          cmp "$output/first/canonical-manifest.json" "$output/second/canonical-manifest.json"
          {
            echo "### Gooo bool.and:1 dogfood"
            echo '- declaration: `examples/language-value-witness/and.gooo`'
            echo '- semantic check: `PASS`'
            echo '- generated Go replay: `IDENTICAL`'
            echo '- generated manifest replay: `IDENTICAL (canonical; output path excluded)`'
            echo '- canonical Boolean input/operand policy: `FAIL_CLOSED`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload bool.and:1 dogfood evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-bool-and-dogfood-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-bool-and-dogfood
          if-no-files-found: error
          retention-days: 14
      - name: Execute the bool.and:1 Gooo plan and replay its receipts
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-bool-and-execution"
          mkdir -p "$output"
          for value in 0 1; do
            printf '{"value":%s}\n' "$value" > "$output/input-$value.json"
            for pass in first second; do
              go run ./cmd/gooo run --json --entry And \
                --input "$output/input-$value.json" \
                examples/language-value-witness/and.gooo > "$output/$value-$pass.json"
            done
            cmp "$output/$value-first.json" "$output/$value-second.json"
            jq -e --argjson expected "$value" '
              .schema == "gooo/value-execution-plan/v1" and
              .decision == "PASS" and
              .entry == "And" and
              .execution.scope == "REGISTERED_VALUE_OPERATION" and
              .execution.results.And.output_entity == "Boolean" and
              .execution.apply_calls == 1 and
              .execution.deliveries == 0 and
              .execution.results.And.value == $expected and
              .execution.results.And.operation_spec_digest != "" and
              .execution.results.And.result_digest != ""
            ' "$output/$value-first.json" > /dev/null
          done
          {
            echo "### Gooo bool.and:1 execution"
            echo '- declaration: `examples/language-value-witness/and.gooo`'
            echo '- inputs: `false (0)`, `true (1)`'
            echo '- observed results: `false (0)`, `true (1)`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- canonical Boolean input/operand policy: `FAIL_CLOSED`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload bool.and:1 execution evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-value-bool-and-execution-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-bool-and-execution
          if-no-files-found: error
          retention-days: 14
      - name: Execute the typed bool.and:1 plan and replay its receipts
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-runtime-boolean-and-plan"
          mkdir -p "$output"
          for input in 0 7; do
            if [ "$input" -eq 0 ]; then
              expected_zero=1
              expected_not=0
              expected_and=0
            else
              expected_zero=0
              expected_not=1
              expected_and=1
            fi
            printf '{"value":%s}\n' "$input" > "$output/input-$input.json"
            for pass in first second; do
              go run ./cmd/gooo run --json --entry IsZero \
                --input "$output/input-$input.json" \
                examples/language-runtime-binding/boolean-and.gooo > "$output/$input-$pass.json"
            done
            cmp "$output/$input-first.json" "$output/$input-second.json"
            jq -e \
              --argjson expected_zero "$expected_zero" \
              --argjson expected_not "$expected_not" \
              --argjson expected_and "$expected_and" '
              .schema == "gooo/value-execution-plan/v1" and
              .decision == "PASS" and
              .entry == "IsZero" and
              .execution.scope == "REGISTERED_VALUE_OPERATION" and
              .execution.apply_calls == 3 and
              .execution.deliveries == 2 and
              .execution.activities == ["IsZero", "Not", "And"] and
              .execution.plan_digest != "" and
              .execution.input_digest != "" and
              .execution.execution_digest != "" and
              .execution.results.IsZero.output_entity == "Boolean" and
              .execution.results.Not.output_entity == "Boolean" and
              .execution.results.And.output_entity == "Boolean" and
              .execution.results.IsZero.value == $expected_zero and
              .execution.results.Not.value == $expected_not and
              .execution.results.And.value == $expected_and and
              .execution.results.IsZero.result_digest != "" and
              .execution.results.Not.result_digest != "" and
              .execution.results.And.result_digest != ""
            ' "$output/$input-first.json" > /dev/null
          done
          {
            echo "### Gooo typed bool.and:1 plan"
            echo '- declaration: `examples/language-runtime-binding/boolean-and.gooo`'
            echo '- typed path: `IsZero -> Not -> And`'
            echo '- inputs: `0`, `7`'
            echo '- plan execution: `3 applies / 2 deliveries` per input'
            echo '- Boolean entity preservation: `PASS`'
            echo '- execution receipt replay: `IDENTICAL`'
            echo '- repository writes: `0`'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload typed bool.and:1 plan evidence
        uses: actions/upload-artifact@v7
        with:
          name: language-runtime-boolean-and-plan-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-runtime-boolean-and-plan
          if-no-files-found: error
          retention-days: 14
      - name: Preserve refuted accepted-revision evidence
        run: go test ./internal/valueexecution -run TestCompareAcceptedRevisionNextRunRefutesTamperedAcceptedReceipt -count=1
      - name: Replay exact Gooo value witness
        shell: bash
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/language-value-witness"
          mkdir -p "$output"
          for pass in first second; do
            go run ./scripts/language-value-witness \
              -source examples/language-value-witness/main.gooo \
              -activity Increment -head-sha "$HEAD_SHA" \
              -output "$output/$pass.json" -check
          done
          cmp "$output/first.json" "$output/second.json"
          {
            echo "### Gooo value witness"
            jq -r '"- decision: `\(.decision)` at `\(.resolution)`"' "$output/first.json"
            jq -r '"- value cases: `\(.summary.value_cases_passed)/\(.summary.value_cases_total)`; outputs: `\(.summary.value_outputs_observed)`"' "$output/first.json"
            jq -r '"- counterexamples: `\(.summary.counterexamples_passed)/\(.summary.counterexamples_total)`; replays: `\(.summary.deterministic_replays)/5`"' "$output/first.json"
            jq -r '"- scoped improvement: `\(.improvement.before.satisfied)/\(.improvement.before.total) -> \(.improvement.after.satisfied)/\(.improvement.after.total)`"' "$output/first.json"
            jq -r '"- reader views: `\(.views[0].satisfied)/\(.views[0].total)`, `\(.views[1].satisfied)/\(.views[1].total)`, `\(.views[2].satisfied)/\(.views[2].total)`"' "$output/first.json"
            jq -r '"- core IR preservation: `\(.summary.core_ir_program_preserved.satisfied)/\(.summary.core_ir_program_preserved.total)`; fingerprint: `\(.summary.core_ir_fingerprint_sensitive.satisfied)/\(.summary.core_ir_fingerprint_sensitive.total)`; unknown-attribute guard: `\(.summary.core_ir_unknown_attribute_fail_closed.satisfied)/\(.summary.core_ir_unknown_attribute_fail_closed.total)`"' "$output/first.json"
            jq -r '"- repository writes: `\(.summary.repository_writes)`; mutation authority: `\(.authority.repository_mutation_authorized)`"' "$output/first.json"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload exact-head value witness
        uses: actions/upload-artifact@v7
        with:
          name: language-value-witness-${{ env.HEAD_SHA }}
          path: ${{ runner.temp }}/language-value-witness/first.json
          if-no-files-found: error
          retention-days: 14
