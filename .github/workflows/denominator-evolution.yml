name: Denominator evolution experiment

on:
  pull_request:
    branches: [dev, main]

permissions:
  contents: read

jobs:
  denominator-evolution:
    name: denominator-evolution
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      GOTOOLCHAIN: go1.27.0
      HEAD_SHA: ${{ github.event.pull_request.head.sha }}
    steps:
      - name: Check out exact pull request head
        uses: actions/checkout@v5
        with:
          ref: ${{ env.HEAD_SHA }}
          fetch-depth: 1
          persist-credentials: false
      - name: Set up Go 1.27
        uses: actions/setup-go@v6
        with:
          go-version: '1.27.0'
          cache: true
      - name: Produce and independently verify denominator receipt
        shell: bash
        run: bash scripts/denominator-evolution/main.sh
